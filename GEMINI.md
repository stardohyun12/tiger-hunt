# GEMINI.md — 이 리포를 읽는 Gemini(agy)에게

## 이 프로젝트가 무엇인가

**OpenAI Game Builders Seoul 2026** 출품용 브라우저 웹 게임. 접수 마감 **2026-08-26**, 1인 제작.

- 스택: **순수 TypeScript + Vite + Canvas2D**. 게임 엔진·프레임워크 없음
- 배포: Cloudflare Workers (로그인 없이 열리는 정적 링크 + 랭킹 API + 1v1 룸)
- **게임 컨셉은 아직 미정이다.** 없는 시스템을 있다고 가정하지 말 것

## 지금 존재하는 것 / 없는 것

리포를 실제로 읽고 판단하라. 2026-08-19 기준으로는 **문서만 있고 코드는 아직 없다.**
`src/sim/rules.ts`(게임 규칙)는 컨셉 확정 전까지 비어 있다.

## 설계의 축 (심판할 때 이걸 기준으로 봐라)

`src/sim/` 은 **순수 결정론 시뮬레이션 코어**다 — 부동소수·난수·시간·DOM 없이 `step(state, inputs) → state` 만 있다.
여기서 네 가지가 파생된다: ①데일리 시드(전세계 같은 판) ②리플레이 고스트 ③lockstep 실시간 1v1 ④**서버에서 리플레이를 재생한 스코어 검증**.
④가 성립하는 이유는 같은 `sim/` 코드가 브라우저와 Cloudflare Worker 양쪽에서 돌기 때문이다.

## 네게 주로 부탁할 것

1. **설계·기획 심판** — 위 구조의 약점, 7일 일정의 비현실적인 부분
2. **아트 프롬프트 작성** — Nano Banana 웹UI 에 넣을 프롬프트. ⚠ 웹UI 는 SDXL 문법이 아니다: **네거티브 프롬프트·CFG·LoRA 가 없다.** 금지는 긍정문으로 뒤집어 쓸 것("no anti-aliasing" → "sharp, aliased pixel transitions"). 키워드 나열보다 서술문이 먹는다
3. 긴 문서 통독

## 이렇게 답해라

- **약점만 짚어라. 아부하지 마라. 전제가 틀렸으면 먼저 말해라**
- 리포에 없는 파일·시스템을 **지어내지 마라.** 모르면 모른다고 해라
- 게임 밸런스 수치나 규칙을 임의로 확정하지 마라 — 후보와 근거를 내고 결정은 사람이 한다
