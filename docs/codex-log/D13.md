- 받은 것: `docs/codex-task-D13.md`의 0~8절에 따라 활 연사 비용, 앞막기 수리, 렌더 전용 속도감, 키 배치 변경, 다섯 봇과 헛스윙 비율 검증, 결정론 3/3과 단일 파일 빌드를 구현·검증하라는 지시를 받았다.
- 구현한 것: `config/state/arrow/player/sim`에 최소 0.25초 차지와 0.45초 재장전·0.75배 이동, 실패 표시 상태를 넣고, `tiger`의 교전 속도를 플레이어 기준 상대속도로 바꾸며 거리 밖 예고 복귀와 접촉 1회 비틀거림을 구현했다. `input/main/render`에는 Space·W·방향키 배치와 재시작 입력 초기화, 상시 조작법, 1.65배 근경·속도선·속도 비례 먼지·촘촘한 지면 마커·±2px 자세를 넣었고, `balance-probe`는 다섯 봇 단일 표와 헛스윙 비율을 출력한다.
- 막힌 곳: 없음. 결정론 테스트는 변경 단계마다 통과했고, 명세 6-3의 스크린샷 5종은 사용자가 빌드를 직접 플레이하기로 해 지시대로 생략했다.
- 사람이 정한 것: 활 연사에 리스크가 없다는 판단, 뒤로 빠질 때 흐름이 끊긴다는 지적, Space 점프가 덜 불편하다는 결정, 기본 이동에도 속도감이 있어야 한다는 요구는 모두 도현의 직접 플레이 판단이다. 대가를 화살 수가 아닌 거리(재장전 감속)로 받고, 호랑이 교전 속도의 기준을 플레이어 속도로 바꾸는 것도 사람이 정한 설계 판단이다.
