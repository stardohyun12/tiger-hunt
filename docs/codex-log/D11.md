- 받은 것: `docs/codex-task-D11.md`의 터치 조작 좌우 분리, 주봉+흘러내리는 2층 산 능선 강화, `tigerBrace`·사슴+기수·활 실루엣 개선, sim 무변경과 D10 밸런스 완전 동일, 실제 캡처 생성 지시를 받았다.
- 구현한 것: `src/config.js`에서 패드를 좌측 뒤(78,478)·앞(188,478), 우측 점프(884,368)·수그림(884,478), 반지름 48로 배치하고 산 봉우리·두 먹 알파·활대와 스프라이트를 조정했다. `src/render.js`는 터치 안내 2줄을 새 배치에 맞추고 비조준 활을 몸 바깥의 뒤쪽으로 펼쳤으며, `docs/art/sprite-preview.html`은 D11 스프라이트와 `tigerBrace` 비교를 동기화했다. `src/input.js`의 멀티터치 역할 분배는 패드 목록 기반이라 수정 없이 새 배치를 적용한다. 측정은 원경 최고 225뷰(41.7%)·인접 열 차 최대 3뷰(1칸), 중경 258뷰(47.8%)·6뷰(2칸), `tigerBrace` 채움 48.4%, 좌측 패드와 후방 호랑이 69뷰(305−236), 우측 패드와 전방 호랑이 86뷰(836−750) 여백이다. 결정론 3/3, D10과 동일한 밸런스 출력, 빌드(63,245바이트), 판정판 14종 동기화가 통과했다.
- 막힌 곳: 인앱 브라우저가 연결 가능한 인스턴스 0개였고 일반 Edge/Chrome 헤드리스는 GPU 프로세스가 반복 종료됐다. Chrome 단일 프로세스 소프트웨어 렌더와 실제 `render()`를 고정 sim 상태에 호출하는 임시 정적 재현 페이지로 우회한 뒤 임시 페이지는 제거했다. `D11-run.png`, `D11-rear.png`, `D11-front.png`, `D11-touch.png`, `D11-touch-title.png`, `D11-sprite-preview.png`를 남겨 산·후방 주홍 예고·전방 `tigerBrace`·분리 패드·상시 노출 조작 문구·판정판을 확인했다.
- 사람이 정한 것: 조작을 좌우로 나누고 배치가 곧 의미가 되게 한 것(왼쪽=뒤/오른쪽=앞, 위=점프/아래=수그리기), 산을 균일 상향하지 않고 주봉과 흘러내리는 능선으로 만든 것, 활이 실루엣에서 읽히게 해 기마궁술 게임의 정체성을 드러낸 것, 기존 픽셀 양식 자체는 유지한 것은 도현의 결정이다.
