- 받은 것: `AGENTS.md`, `docs/design/HANDOFF.md`, `docs/codex-task-D2.md`의 결정론·구조 계약과 기존 게임 감각/밸런스 보존 지시.
- 구현한 것: 고정 1/60초 sim과 5스텝 누적기, 정수 프레임 입력, xorshift32 PRNG, sim 이벤트/FX 분리, 델타 리플레이·FNV-1a 해시, 결정론 테스트 3종과 단일 HTML 빌드 반영.
- 막힌 곳: 인앱 브라우저 연결이 시간 초과되어 실제 UI 수동 플레이는 못 했고, 대신 헤드리스 규칙 시뮬레이션과 빌드 번들의 시작·조준/차지/발사·수그리기·점프 입력 스모크로 런타임을 검증함.
- 사람이 정한 것: 순수 JavaScript ESM/무의존성 빌드 유지, `config.js` 기존 튜닝값 유지, 텔레그래프 0.5초와 호랑이의 현재 자세 거울 규칙 유지, 커밋하지 않음.

---

## Claude 검산 (2026-08-19)

Codex 의 "3/3 통과" 주장을 그대로 믿지 않고 독립적으로 확인했다.

| 검사 | 결과 |
|---|---|
| `node --test tests/determinism.test.mjs` | 3/3 통과 |
| `node build.mjs` | `dist/play.html` 28,046 bytes |
| **음성 대조군** — `tiger.js` 맨 위에 `Math.sin(1)` 주입 | `tiger.js:1: 금지 Math 함수` 로 **파일·줄 번호까지 찍고 실패**. 정적 스캔이 실제로 작동한다 |
| **공허성 검사** — `hashState` 가 상수를 뱉으면 테스트 1·2 가 자동 통과한다 | 시드 5개 → 서로 다른 해시 5개. 장애물 수·gap 도 시드마다 다르다 (데일리 시드가 성립한다) |
| `config.js` 밸런스 수치 | 변경 없음. 추가만 (`FIXED_DT`, `FIXED_STEPS_MAX`, `INPUT_KEY`) |
| 의존 방향 위반 3건 | 전부 해소. `arrow.js` 에서 `input.js` 제거 · `tiger.js`/`obstacle.js` 에서 `fx.js` 제거 · `drawAimPreview` → `render.js` |

**설계 판단이 옳았던 지점**: `hitstop` 을 `sim.js` 안에서 적용한 것. hitstop 은 게임 시간축을 멈추므로 sim 이 소유해야 하고, 그래야 `playback()` 이 라이브 플레이와 같은 프레임에서 멈춘다. trauma·진동만 presentation 에 남긴 분리가 정확하다.

### 남은 것 (D3 에서 정리)

1. **`fx.js` 의 `triggerFx` 가 아직 `S.hitstop` 을 쓴다.** `sim.js` 가 이미 같은 값을 적용한 뒤라 지금은 멱등이라 무해하지만, **게임플레이 로직이 presentation 레이어에 중복돼 있다.** 한쪽만 고치면 갈라진다 — `triggerFx` 에서 hitstop 줄을 걷어내야 한다.
2. **실제 브라우저 플레이가 아직 검증되지 않았다.** Codex 의 인앱 브라우저 연결이 시간 초과됐고, 나도 확인하지 않았다. 고정 timestep 전환 뒤 **감각이 이전과 같은지는 사람이 손으로 확인해야 한다** — 이게 D2 의 진짜 완료 판정이다.
