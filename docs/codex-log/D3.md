- 받은 것: `docs/codex-task-D3.md`의 순서대로 넉백·수그리기 버그, 추월 후 차단하는 교전 구조, Canvas2D 벡터 렌더링을 구현하고 단계마다 결정론을 검증하라는 지시.
- 구현한 것: `config.js`·`state.js`·`player.js`·`tiger.js`·`fx.js`·`render.js`에 방향성 넉백, 7상태 교전·몸통 차단·두 취약 구간, 중앙 카메라, 3층 패럴랙스·지면 마커·동물 자세 애니메이션·스피드라인·2프레임 플래시·새 흔들림 곡선을 구현.
- 막힌 곳: 연결 가능한 브라우저 인스턴스가 없어 `dist/play.html`의 실제 화면 확인은 못 했고, 대신 타이틀과 교전 7상태 Canvas 렌더 스모크·문법 검사·빌드로 런타임 경로를 검증함.
- 사람이 정한 것: 추월→차단→뒤돌기 순서, `blockSpeed` 등 D3 시작값, 0.5초 텔레그래프와 현재 자세를 거울로 노리는 규칙 유지, 소리는 이번 작업에서 제외한다는 범위.

---

## Claude 검산 (2026-08-19 밤)

- 테스트 3/3 · 빌드 37.5KB 확인
- 헤드리스 60초: chase→overtake→brace→windup→swing→recover→cooldown 전 상태 전이 확인, 프레임당 최대 이동 7.2px (순간이동 소멸)
- **발견한 버그 2건 → D4 로 이관** (지시서의 수치 결함, Codex 잘못 아님):
  1. blockGap(210)+brace 관성으로 정지 거리가 -246 — reach(195) 밖이라 **무입력 플레이어가 영원히 안 맞는다**
  2. 호랑이가 앞에 있을 때 chase 감속 규칙이 없어 장애물에 걸린 플레이어와 격차가 -1293px 까지 벌어짐 (사실상 이탈)
