- 받은 것: `docs/codex-task-D12.md`의 강무 온보딩 틀, 월드 2,000~3,000px 사이 호랑이 난입, seeded 과녁 2~3단, 명중 질주·콤보, 거리 점수 축소, 과녁 봇 probe 비교, 주홍 위험 규율, 캡처 5종과 결정론 3/3·빌드 검증 지시를 받았다.
- 구현한 것: `src/target.js`를 추가해 seeded 3단 과녁이 연속으로 같은 높이에 나오지 않게 스폰하고, 통과·빗맞음 콤보 초기화, 명중 점수(80점×최대 5배), 0.65초·430px/s 질주와 피드백 상태를 구현했다. `state/tiger/sim/arrow/player`에는 2,500px `offstage` 난입과 과녁 판정·질주를 연결하고, 거리 점수 간격은 10→30px로 낮췄다. `render/config`에는 먹+한지 과녁, 명중 때만 주홍 잔상·먼지·점수 팝업, 콤보 HUD, 1.5초 난입 경고와 강무 부제를 넣었다. `balance-probe`는 실제 고정 timestep 탄도로 조준하는 과녁 봇과 합격 판정을 추가했으며 과녁 봇 8,575점 > D 홀드 400점, 무입력 사망 50.9초, 난입 전 교전 0f, 활성 `|gap|` 최대 620px를 확인했다. 결정론 3/3(리플레이 왕복 포함), 빌드 69,789바이트와 캡처 `D12-pre/enter/post/combo/title.png`가 통과했다.
- 막힌 곳: 인앱 브라우저 연결과 복구 문서 조회가 각각 2분 동안 응답하지 않았다. D11에서 검증된 Chrome 단일 프로세스+SwiftShader 경로와 실제 `render()` 고정 상태 fixture로 우회해 난입 전 과녁만, 난입 경고, 난입 후 교전+과녁, 콤보 HUD·팝업, 타이틀을 확인했고 임시 서버·fixture·프로필은 제거했다.
- 사람이 정한 것: 강무 중 호랑이 난입이라는 틀(도현)로 과녁의 존재 이유와 온보딩을 동시에 푼 것, 과녁 명중에 질주 보상을 붙여 “실력이 곧 거리”가 되게 한 것, 거리 점수를 깎아 도망이 최적이 아니게 만든 것, 주홍은 위험 전용이라 평상시 과녁에는 쓰지 않은 것은 사람이 정했다.
