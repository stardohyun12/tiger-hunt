- 받은 것: `docs/codex-task-D9.md`의 호랑이 예고 자세 재설계, 칸형 텔레그래프, 판정판 수치화와 Edge 상태 캡처 지시를 받았다.
- 구현한 것: `src/config.js`·`src/render.js`·`docs/art/sprite-preview.html`에서 `tigerWindup`/`tigerSwing`을 40×25 바닥 정렬로 다시 그리고, 8칸 진행 띠·상하 캡·사슴 표식을 구현했다. 채움 비율은 `tigerRunA` 54.9%, `tigerWindup` 51.4%, `tigerSwing` 49.4%이며, `docs/codex-log/D9-*.png` 8장으로 후방 25/60/95%, 전방, swing, 상단/하단, 판정판을 확인했다.
- 막힌 곳: 인앱 브라우저 연결이 반복 시간 초과되고 Edge 헤드리스 GPU 프로세스가 종료되어, 로컬 정적 상태 재현 페이지와 Chrome SwiftShader 캡처로 같은 렌더 경로를 검증했다. 캡처 8장 모두 렌더 오류 표식 없이 생성됐고 런타임 예외는 0건이었다.
- 사람이 정한 것: 예고 자세를 채움 비율로 판정(≤55%, `tigerRunA` 기준), 치켜든 앞발만 판정 상자 위로 예외 허용, 텔레그래프를 점멸 대신 남은 시간을 칸으로 채우는 방식으로 바꿔 타이밍을 몸으로 읽게 한 것, 상단/하단을 캡 바 위치로 구분.
