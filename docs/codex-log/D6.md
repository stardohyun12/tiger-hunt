- 받은 것: `docs/codex-task-D6.md`의 새 교전 설계대로 평소에는 후방에서 바로 예고·후려치기하고, 실제 위치가 `bodyGap`까지 붙었을 때만 추월·차단 경로로 전환하며 결정론·거리 클램프·순간이동 상한과 실제 캔버스를 검증하라는 지시.
- 구현한 것: `src/tiger.js`에서 `chase → windup` 후방 경로와 위치 기반 `startOvertake()`를 분리하고, 추월 진입 프레임에 stumble 이벤트만 1회 부여했으며 프레임 내 접근·발톱 넉백으로 접촉선을 넘는 경우도 `overtake → brace`로 연결함; `src/config.js` 설명을 새 의미에 맞췄고 `engageGap` 175·`reach` 195는 실제 캡처에서 후방 앞발/텔레그래프 범위가 맞아 수치를 유지함.
- 막힌 곳: 브라우저 플러그인 연결 런타임이 반복 시간 초과되어 D4·D5와 같은 설치형 Edge 151 헤드리스 CDP로 대체함; 무입력 사망 49.000초, D/A 60초 최대 `|gap|` 620, A 추월 1.983초, 호랑이 최대 이동 7.166667px/프레임, 전 상태 전이·런타임 예외 0건과 후방 정방향/앞발 방향/0.5초 텔레그래프를 확인함.
- 사람이 정한 것: 평소 후방 교전·후퇴 시에만 추월(도현), 추월 트리거를 입력 플래그가 아니라 위치로 판정, 밀치기는 stumble만 주고 HP는 깎지 않음, `overtake`/`recover` 취약 구간을 유지해 “일부러 물러나 큰 피해를 꽂는” 선택지를 살림.
