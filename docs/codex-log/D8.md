- 받은 것: `docs/codex-task-D8.md`에 따라 sim 파일은 건드리지 않고 민화 4색을 유지한 공룡게임식 픽셀 렌더, 문자열 스프라이트, 독립 판정 미리보기, 실제 Edge 캡처를 구현하라는 지시.
- 구현한 것: `src/config.js`의 중단된 `rig`를 제거하고 14개 문자열 스프라이트·렌더 수치를 `art`에 추가했으며, `src/render.js`를 런 렝스 `blit`·거리 기반 2프레임·계단형 원경·지면 텍스처·격자 그림자/먼지/화살·사각 텔레그래프로 교체하고 `index.html` 팔레트/CSS와 `docs/art/sprite-preview.html`의 1×/4×·실루엣·배경·교대 재생 판정판을 완성함; D8 캡처 8장으로 각 상태를 검증함.
- 막힌 곳: 브라우저 플러그인 런타임이 두 차례 시간 초과되고 첫 Edge GPU 설정도 실패했으나, 설치된 Edge 151을 소프트웨어 렌더링 가능한 헤드리스 설정으로 실행해 타이틀·후방/전방 교전·점프·수그리기·게임오버·터치·스프라이트 판정판을 캡처했고 6개 제어 상태의 런타임 예외 0건을 확인함.
- 사람이 정한 것: 배경을 어두운색에서 한지로 뒤집어 공룡게임 문법과 민화를 동시에 만족시킨 것, 4색에 직업을 하나씩만 준 것(주홍=위험 전용), 다리를 해부학 대신 2프레임 교대로 푼 것, 활·조준 궤적만 격자 예외로 둔 것.
