- 받은 것: `docs/codex-task-D4.md`의 순서대로 무입력 헛스윙·전방 chase 이탈 버그를 먼저 고치고, 민화 톤 타이틀·HUD·게임오버와 점수·최고 기록을 구현한 뒤 단계별 결정론 및 60초 헤드리스 전이를 검증하라는 지시.
- 구현한 것: `config.js`·`state.js`·`tiger.js`·`arrow.js`·`sim.js`·`replay.js`·`main.js`·`render.js`에 windup 방향성 접근, 전방 chase 감속, 거리·명중·강명중·처치 점수, 0입력 타이틀 데모, 벡터 조작표·하트·점수·차지·게임오버 UI와 `main.js` 한정 최고 기록 저장을 구현.
- 막힌 곳: 내장 브라우저 연결이 반복 시간 초과됐지만 설치된 Edge의 헤드리스/CDP로 대체해 실제 Canvas 네 화면, Space 전이, 차지 게이지, 런타임 예외 0건과 최고 기록 저장을 확인했고 시작 버튼 여백도 캡처를 보고 보정함.
- 사람이 정한 것: 접근 속도 115와 전방 chase `blockSpeed × 0.7` 시작값, 점수 배점 10px당 1·명중 25·강명중 60·처치 500, 민화 팔레트·UI 필수 문구, 0.5초 텔레그래프와 현재 자세를 거울로 노리는 규칙을 유지한다는 결정.

---

## Claude 검산 (2026-08-19 밤)

- 테스트 3/3 · 빌드 42.5KB
- 버그 1-a ✔ 무입력 플레이어 40.2초 사망 (windup 중 approachSpeed 접근)
- 버그 1-b: aheadChaseMul(0.7=175) 만으로는 A홀드(120) 시 초당 55씩 이탈 → **Claude 가 tiger.js 에 전방 대칭 클램프 1줄 추가** (`T.x = Math.min(T.x, S.worldX + K.gapMax)`). 지시서가 예시값만 주고 후퇴 케이스를 안 다룬 결함. 재검증: A홀드 60초 최대 |gap| = 620 ✔
