# CLAUDE.md — 이 프로젝트 고유 규칙

> 3중 배선(Claude·Codex·Gemini) 규약은 전역 `~/.claude/CLAUDE.md` 에 있다. 여기엔 이 프로젝트만의 것을 쓴다.

## 이 프로젝트

**《호랑이 추격》** — OpenAI Game Builders Seoul 2026 출품작. 2D 사이드뷰 기마궁술 액션.

| | |
|---|---|
| 접수 마감 | **2026-08-26** (본선은 8/31 — 접수일이 아니다) |
| 팀 | 도현 1인 |
| 스택 | **순수 JS ESM + `build.mjs` → `dist/play.html` 단일 파일 ~22KB.** 의존성 0. Vite·TS 도입 금지 |
| 설계 정본 | `docs/design/HANDOFF.md` — 유일한 자원은 **거리(gap)** |
| 원본 | claude.ai 웹에서 만든 프로토타입(725줄). `claude_web/` 에 스냅샷, git 제외 |
| 계획 원본 | `~/.claude/plans/https-openaigame2026-com-main-twinkly-shamir.md` |

도현의 방침: **"일단 만들어보고 고친다."** 규칙을 확정 취급하지 말되, 바꿀 땐 `HANDOFF.md` 의 "손대지 말아야 할 것"을 먼저 볼 것.

## 이 프로젝트에서의 역할 분담

전역 규약의 "쓰는 자 하나, 보는 자 둘"에서 **이 프로젝트만 예외**가 있다:

- **코드는 Codex 가 전량 쓴다** (Orca 안에서). 심사 항목에 "Codex Collaboration" 이 있어서 내린 결정이다
- **나(Claude)는**: 설계 · 검산 · 에셋 규격 판정 · 문서/기록 · 배포 배관
- 내가 `src/` 를 직접 고치는 건 Codex 가 막혔을 때뿐이고, 그때는 `docs/codex-log/` 에 그 사실을 남긴다

## 절대 규칙

결정론 불변식은 **`AGENTS.md` 가 정본**이다. 중복해서 쓰지 않는다.
요약: **float 는 허용**(IEEE754 기본연산은 정확히 규정 + Chrome·Node·Workers 전부 V8). 대신 **초월함수 금지 · 고정 timestep 1/60 · seeded PRNG · 입력 정수 양자화**.

이게 깨지면 리플레이·고스트·실시간 1v1·서버측 스코어 검증이 **전부** 거짓말이 된다. 이 넷이 이 출품작의 차별점이므로 다른 무엇보다 우선한다.

## 에셋

- **전부 새로 만든다.** 다른 리포(`kingdom-cards*`, `HD2D_Project` 등)에서 가져오지 않는다 — 아트 방향이 끌려가지 않게 하려는 도현의 결정이다
- 그림 = Nano Banana 웹UI (프롬프트는 `agy` 로 작성) · SFX = **WebAudio 코드 합성** · BGM 은 후순위
- **그림은 반드시 도현이 직접 보고 판정한다.** 내가 Read 로 혼자 보고 결론만 전하지 않는다 — HTML 뷰어를 구워 열고, **1× → 흑실루엣 → 4×** 순서로 본다
- 리샘플은 딱 한 번(원본→논리해상도, BOX). 이후는 NEAREST 정수배 확대만

## 기록

`docs/codex-log/D<n>.md` 는 **제출물이다**(가산점 항목: "Codex 활용 과정 · 인적 결정 부분"). 사후에 지어내지 말고 그날 남긴다.

## 제출 요건 (잊으면 실격)

- 플레이 링크가 심사 기간 내내 살아 있고 **로그인 불필요**, 화면에 **조작법**이 보일 것
- 16:9 썸네일 (≤10MB)
