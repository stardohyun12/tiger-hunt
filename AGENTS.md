# AGENTS.md — 이 리포에서 코드를 쓰는 에이전트가 지켜야 할 규약

이 파일은 **Codex 가 매 세션 읽는다.** 여기 적힌 규칙이 개별 작업 지시보다 우선한다.

---

## 0. 무엇을 만드는가

**《호랑이 추격》** — OpenAI Game Builders Seoul 2026 출품작. 브라우저에서 바로 실행되는 2D 사이드뷰 기마궁술 액션.
마감 **2026-08-26**. 1인 제작. 코드는 Codex 가 쓴다.

**게임 설계의 정본은 `docs/design/HANDOFF.md` 다.** 규칙을 바꾸기 전에 반드시 읽어라.
한 줄 요약: **이 게임의 유일한 자원은 거리(gap)다.** 장애물·조준·후퇴가 전부 거리 손실로 수렴하고, 명중이 거리를 회복시킨다. 새 기능을 넣기 전에 "이게 거리에 어떻게 연결되는가"를 먼저 답해라.

### 스택 — 지금 있는 그대로가 정답이다

- **순수 JavaScript ESM.** 프레임워크·게임엔진·런타임 의존성 **0**
- 빌드는 `build.mjs` 하나. `node build.mjs` → `dist/play.html` **단일 파일 ~22KB**
- **Vite 를 도입하지 마라. TypeScript 로 변환하지 마라.** 이미 도는 무의존성 빌드가 더 낫다.
  타입 안전이 필요하면 파일 맨 위에 `// @ts-check` 와 JSDoc 을 붙여라 (변환 비용 0)
- `index.html` 은 ES 모듈이라 `file://` 로 안 열린다. 확인은 `dist/play.html` 또는 로컬 서버로

---

## 1. 결정론 — 이 프로젝트의 유일한 하드 제약

리플레이·고스트·실시간 동시주행·**서버측 스코어 검증**이 전부 여기에 얹힌다. 깨지면 넷이 동시에 거짓말이 된다.

### float 는 써도 된다 (중요)

IEEE 754 **기본 연산**(`+ - * /`, `Math.sqrt`)은 표준이 결과를 정확히 규정한다 → 구현이 달라도 비트가 같다.
구현마다 갈리는 건 **초월함수**뿐이다. 게다가 Chrome · Node · Cloudflare Workers 는 **전부 V8** 이다.
그래서 고정소수점으로 갈아엎을 필요가 없다. **대신 아래 넷을 반드시 지켜라.**

### (1) 초월함수 금지 — sim 로직 안에서

| 금지 | 대신 |
|---|---|
| `Math.sin` `cos` `tan` `asin` `acos` `atan` `atan2` | 벡터를 직접 다뤄라. 각도가 꼭 필요하면 렌더에서만 |
| `Math.pow` `**` `Math.exp` `Math.log` `Math.cbrt` | 정수 지수는 곱셈으로 (`x*x`) |
| `Math.hypot` | `Math.sqrt(x*x + y*y)` — hypot 은 구현 근사가 허용돼 있다 |

허용: `+ - * /` · `Math.sqrt` · `Math.min` `max` `abs` `floor` `ceil` `round` `trunc` `sign` · 비교연산

### (2) 고정 timestep

논리 업데이트는 **1/60초 고정**이다. `main.js` 는 누적기(accumulator)로 실시간을 모아 고정 dt 로 여러 번 `update` 를 돌리고, 렌더는 프레임레이트대로 자유롭게 돈다.

```js
export const FIXED_DT = 1 / 60;   // sim 은 이 상수만 안다
```

**sim 함수에 가변 `dt` 를 인자로 넘기지 마라.** 지금 코드는 넘기고 있다 — 이걸 걷어내는 게 D2 작업이다.

### (3) 난수는 seeded PRNG 만

- `Math.random()` 은 **sim 안에서 금지**. 장애물 배치·모든 게임 난수는 시드에서 나온다
- 예외: `fx.js` 의 화면 흔들림은 게임플레이에 영향이 없으므로 허용 (단 sim 상태를 건드리지 않을 것)
- PRNG 는 정수 연산만(xorshift32 / mulberry32 정수판), 상태를 인자로 받아 새 상태를 반환하는 순수 함수형

### (4) 입력은 프레임 단위 정수

- 매 논리 프레임의 입력을 `{frame, keys, aimX, aimY, fire}` 같은 **정수 필드**로 기록한다
- 마우스 조준은 **정수로 양자화**해서 sim 에 넣는다 (예: 뷰포트 좌표를 정수로 반올림)
- **sim 모듈이 `input.js` 를 직접 import 하지 마라.** 지금 `arrow.js` 가 `mouse` 를 직접 읽고 있다 — 이것도 D2 에 걷어낸다

### 테스트가 이걸 강제한다

```bash
node --test tests/determinism.test.mjs
```
1. 같은 시드 + 같은 입력열 → 같은 최종 상태 해시 (시드 100개)
2. 리플레이 직렬화 → 역직렬화 → 재생 결과가 원본과 일치
3. **정적 스캔**: sim 소스에 금지 패턴(위 표 + `Math.random` + `Date.now` + `performance.now` + `document` + `window`)이 있으면 **파일·줄 번호를 찍고 실패**

---

## 2. 구조 계약

현재 `src/` 는 평평하다. **의존 방향이 어긋나 있고, 바로잡는 게 D2 작업이다.**

```
목표 방향 (한쪽으로만 흐른다)

  input ─┐
         ├─→ [ sim ] ←── render (읽기만)
  net  ──┘
```

- **sim**: `config.js` `state.js` `player.js` `tiger.js` `arrow.js` `obstacle.js`
- **presentation**: `render.js` `viewport.js` `fx.js` `input.js`
- **wiring**: `main.js`

현재 위반 (D2 에 고칠 것):
- `arrow.js` 가 `input.js` 의 `mouse` 를 직접 읽는다 → 입력을 인자로 받아야 한다
- `arrow.js` 에 `drawAimPreview`(렌더)가 들어 있다 → `render.js` 로 옮긴다
- `tiger.js` `obstacle.js` 가 `fx.js` 의 `triggerFx` 를 호출한다 → sim 은 **이벤트를 반환**하고 `main.js` 가 fx 를 친다

`worker/` 는 sim 모듈을 **그대로 import** 해서 리플레이를 재생한다 — 그래서 sim 은 브라우저 API 를 몰라야 한다.

---

## 3. 프로토타입에서 물려받은 규칙 (계속 지킨다)

`docs/design/HANDOFF.md` 에서 온 것들이다:

- **숫자는 `config.js` 에만.** 다른 파일에서 상수를 새로 만들지 마라
- **상태는 `S` 객체 하나에만.** 모듈 안에 지역 상태를 두지 마라
- `render.js` 는 읽기 전용. 여기서 상태를 바꾸지 마라
- hitstop 은 `update` 만 멈추고 `render` 는 계속 돈다. `main.js` 의 분기 순서를 바꾸지 마라
- **텔레그래프 0.5초**는 읽기 가능한 하한선이다. 줄이려면 플레이 검증 후에
- **"호랑이가 지금 자세를 노린다"** 규칙을 랜덤으로 바꾸지 마라 — 읽는 재미가 사라진다
- 체력바 같은 **다른 자원을 늘리는 방향**은 피해라. 자원은 거리 하나다

---

## 4. 하지 말 것

- 게임 밸런스 수치를 **임의로 확정하지 마라.** 후보와 근거를 내고 사람이 고른다
- 라이브러리를 추가하지 마라 (필요하면 먼저 물어라)
- 커밋·푸시는 요청받았을 때만
- `claude_web/` 을 읽거나 고치지 마라 — 원본 스냅샷 보관용이고 git 에서 제외돼 있다
- 에셋(그림)을 코드로 흉내내 채우지 마라. 개발 중에는 **도형 플레이스홀더** 유지, 스프라이트로 갈아끼울 수 있게 렌더를 짜라

---

## 5. 작업 기록 — 이것도 제출물이다

세션마다 `docs/codex-log/D<n>.md` 에 네 줄:

```markdown
- 받은 것:       (어떤 명세/지시를 받았나)
- 구현한 것:     (파일과 대략의 규모)
- 막힌 곳:       (문제와 해결 방법)
- 사람이 정한 것: (에이전트가 아니라 사람이 결정한 부분)
```

마지막 항목이 특히 중요하다. 대회 요건에 **"인적 결정 부분을 명시"** 가 박혀 있다.
